// Generated by cloudflare/build.py; edit worker-core.mjs and content/packages.py.
const BUNDLE_ORDER = ["basic", "standard", "vip", "premium"];
const BUNDLE_LABELS = {"basic": "Basic", "standard": "Standard", "vip": "VIP", "premium": "Premium"};
const SERVICE_LABELS = {"website": "🌐 Сайт", "bot": "🤖 Telegram-бот"};
const PACKAGES = {"website": {"basic": {"title": "Basic Bundle — быстрый старт", "subtitle": "Лендинг или небольшой презентационный сайт без сложной серверной логики.", "features": ["1–3 основных страницы или одностраничный landing page", "Адаптивный дизайн для телефона, планшета и компьютера", "Аккуратная структура: главный экран, преимущества, услуги, о компании, контакты", "Кнопки связи: Telegram, WhatsApp, телефон, email и соцсети", "Форма заявки или обратной связи", "Базовые анимации и интерактивные элементы", "Подключение домена и размещение на хостинге при наличии доступа", "Базовая SEO-настройка: title, description, favicon, Open Graph", "Базовая защита форм от спама и некорректного ввода", "1 раунд небольших правок после демонстрации"], "gets": "Готовый аккуратный сайт, который можно использовать как визитку, рекламу или точку входа для клиентов."}, "standard": {"title": "Standard Bundle — полноценный бизнес-сайт", "subtitle": "Многостраничный сайт с более глубокой структурой, формами и базовой динамикой.", "features": ["До 5–8 основных страниц", "Индивидуальная адаптивная вёрстка и более проработанный UI/UX", "Frontend с интерактивными блоками, фильтрами или динамическими секциями", "Backend для обработки форм и базовой бизнес-логики", "Подключение базы данных при необходимости", "Формы заявок, бронирования, обратного звонка или регистрации интереса", "Интеграция Telegram-уведомлений о новых заявках", "Базовая административная возможность изменять контент либо удобная CMS-конфигурация", "Google Maps/карта, соцсети, мессенджеры и внешние сервисы", "Более глубокая SEO-подготовка страниц", "Базовые меры безопасности для форм, API и административных точек входа", "До 2 раундов правок", "Короткая пост-релизная техническая поддержка"], "gets": "Полноценный рабочий веб-сайт для бизнеса, который уже может собирать лиды и выполнять практические задачи."}, "vip": {"title": "VIP Bundle — кастомный продукт", "subtitle": "Индивидуальный сайт с личными кабинетами, автоматизацией и несколькими интеграциями.", "features": ["Расширенная структура сайта без жёсткого ограничения типовыми блоками", "Индивидуальный UI/UX под бренд и задачу клиента", "Полноценный frontend и backend", "Проектирование и подключение базы данных", "Регистрация, авторизация, восстановление пароля и роли пользователей", "Личный кабинет пользователя", "Административная панель для управления данными и контентом", "Каталог, фильтры, поиск, заявки, бронирования, корзина или другие модули", "Интеграции с Telegram, CRM, email, картами или внешними API", "Онлайн-оплата, если она нужна проекту", "Расширенная серверная валидация, разграничение доступа, защита API", "Логирование ключевых действий и ошибок", "Оптимизация скорости и базовая подготовка к масштабированию", "До 3 раундов согласованных правок", "Расширенная техническая поддержка после запуска"], "gets": "Кастомный веб-продукт, который автоматизирует часть бизнеса и может работать с пользователями, данными и внешними сервисами."}, "premium": {"title": "Premium Bundle — максимальная разработка", "subtitle": "Сложный веб-сервис или коммерческая платформа с архитектурой под рост.", "features": ["Полностью индивидуальное проектирование продукта и пользовательских сценариев", "Уникальный дизайн и сложный интерактивный frontend", "Полноценная backend-архитектура и API", "Проектирование масштабируемой структуры базы данных", "Несколько типов пользователей, роли и гибкие права доступа", "Личные кабинеты, административные панели и внутренние рабочие интерфейсы", "Сложные бизнес-процессы: статусы, очереди, заявки, заказы, подписки, документы", "Несколько сторонних интеграций и API", "Платёжные системы, уведомления, email, Telegram, CRM и другие сервисы", "Аналитика и журналирование ключевых событий", "Продвинутая защита: контроль доступа, валидация, безопасная работа с сессиями/токенами", "Оптимизация производительности, кеширование и подготовка инфраструктуры к росту нагрузки", "Развёртывание и настройка production-окружения", "Документация по основным компонентам проекта", "Приоритетная поддержка и согласованный период сопровождения"], "gets": "Полноценный цифровой продукт, который можно развивать как отдельный бизнес или важную внутреннюю систему компании."}}, "bot": {"basic": {"title": "Basic Python Bot — простой бот", "subtitle": "Небольшой Telegram-бот с меню, командами и линейными сценариями.", "features": ["Команда /start и приветственный сценарий", "Inline/Reply-кнопки и понятная навигация", "До нескольких основных разделов меню", "Пошаговые сценарии «вопрос → ответ»", "Сбор имени, username, контакта и другой простой информации", "Отправка готовой заявки администратору", "Простая валидация пользовательского ввода", "Команды «Назад», «Главное меню», «Отменить»", "Базовая обработка ошибок", "Развёртывание бота на сервере при наличии инфраструктуры", "1 раунд небольших правок"], "gets": "Простой Telegram-помощник, который заменяет ручной приём типовых сообщений и заявок."}, "standard": {"title": "Standard Python Bot — рабочая автоматизация", "subtitle": "Бот с базой данных, состояниями пользователей и полноценным процессом обработки заявок.", "features": ["Всё из Basic", "База данных пользователей и заявок", "FSM/состояния: бот понимает, на каком шаге находится пользователь", "Регистрация пользователя или сбор профиля", "История заявок/действий при необходимости", "Расширенные формы и ветвящиеся сценарии", "Уведомления администратору или менеджеру", "Базовые админ-команды внутри Telegram", "Поиск, фильтрация или просмотр записей в рамках задачи", "Плановые уведомления/напоминания простого уровня", "Интеграция с одним внешним API или сервисом при необходимости", "Логирование ошибок и ключевых событий", "Базовая защита административных функций по Telegram ID", "До 2 раундов правок и короткая поддержка после запуска"], "gets": "Бот, который уже выполняет роль рабочего инструмента и хранит структуру взаимодействия с клиентами."}, "vip": {"title": "VIP Python Bot — кастомная система", "subtitle": "Продвинутый бот с несколькими ролями, платежами/интеграциями и сложной логикой.", "features": ["Всё из Standard", "Несколько ролей: клиент, менеджер, администратор и другие", "Расширенная административная панель внутри Telegram или отдельный web-admin", "Платежи и проверка статусов оплаты через доступного провайдера", "Подписки, тарифы, доступ к закрытым разделам или контенту", "Каталог товаров/услуг, корзина, заявки, заказы или бронирования", "Автоматические уведомления пользователям и менеджерам", "Интеграции с CRM, Google Sheets, email, сайтом или другими API", "Генерация файлов, счетов, отчётов или документов по задаче", "Фоновые задачи и очереди там, где это действительно нужно", "Система журналов, статусов и истории операций", "Разграничение прав доступа и серверная проверка критичных операций", "Защита webhook/API, секретов и административных функций", "До 3 раундов согласованных правок", "Расширенный период технической поддержки"], "gets": "Индивидуальная Telegram-система, которая автоматизирует заметную часть работы бизнеса."}, "premium": {"title": "Premium Python Bot — бизнес-платформа", "subtitle": "Высоконагруженный или многофункциональный бот с отдельным backend, интеграциями и архитектурой под масштабирование.", "features": ["Всё необходимое из VIP, но архитектура проектируется индивидуально", "Отдельный backend/API для общей бизнес-логики", "Масштабируемая база данных и продуманная модель данных", "Сложные пользовательские сценарии и многоуровневые роли", "Интеграция нескольких внешних сервисов и API", "Webhook-архитектура, фоновые процессы, очереди и планировщики", "Web-admin/dashboard для команды при необходимости", "Аналитика, отчёты, аудит событий и история критичных операций", "Кеширование и оптимизация частых операций", "Продвинутая обработка ошибок и мониторинг состояния сервиса", "Раздельное хранение секретов и конфигураций окружений", "Ограничение доступа, защита API/webhook, rate limiting", "Подготовка production-развёртывания и документация", "Архитектура с возможностью добавлять Mini App, сайт, мобильный интерфейс в будущем", "Приоритетное сопровождение и доработки в рамках согласованного объёма"], "gets": "Не просто Telegram-бот, а масштабируемая платформа с Telegram как основным или одним из ключевых интерфейсов."}}};
const SURVEY_QUESTIONS = {"website": [{"key": "site_name", "question": "Вопрос 1. Как вы хотите назвать ваш сайт?", "hint": "Если название ещё не придумано, можно ответить «Не знаю»."}, {"key": "site_topic", "question": "Вопрос 2. С чем будет связан сайт?", "hint": "Например: ресторан, онлайн-магазин, обучение, компания, услуги, личный бренд."}, {"key": "site_description", "question": "Вопрос 3. Опишите свой сайт кратко.", "hint": "Свободно опишите идею, желаемые функции, страницы и внешний вид."}], "bot": [{"key": "bot_name", "question": "Вопрос 1. Как вы хотите назвать Telegram-бота?", "hint": "Если название ещё не определено, можно ответить «Не знаю»."}, {"key": "bot_purpose", "question": "Вопрос 2. Для чего нужен бот?", "hint": "Например: продажи, приём заявок, обучение, поддержка, каталог, автоматизация, подписка."}, {"key": "bot_audience", "question": "Вопрос 3. Кто будет пользоваться ботом?", "hint": "Клиенты, сотрудники, ученики, подписчики или несколько типов пользователей."}, {"key": "bot_features", "question": "Вопрос 4. Какие основные функции должны быть в боте?", "hint": "Перечислите кнопки, сценарии, оплату, базу данных, уведомления, интеграции и т.д."}, {"key": "bot_description", "question": "Вопрос 5. Опишите желаемую работу бота своими словами.", "hint": "Финальное свободное описание логики проекта."}]};
export const escapeHtml = value => String(value ?? '').replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;');
const button = (text, callback_data) => ({text, callback_data});
const kb = rows => ({inline_keyboard: rows});
const mainMenu = () => kb([[button('🌐 Заказать сайт', 'service:website')], [button('🤖 Заказать Telegram-бота', 'service:bot')]]);
const cancelMenu = () => kb([[button('❌ Отменить', 'survey:cancel')]]);
const previewMenu = () => kb([[button('✅ Готово', 'preview:confirm')], [button('✏️ Заполнить заново', 'preview:restart')], [button('❌ Отменить', 'preview:cancel')]]);
const welcome = '👋 Привет! Это бот <b>Goshapa</b> для приёма заказов на сайты и Telegram-ботов.\n\nЗдесь вы можете:\n• выбрать пакет разработки сайта или Telegram-бота;\n• посмотреть, что входит в каждый пакет;\n• оставить заявку в пару шагов.\n\nВыберите, что хотите заказать 👇';
const initial = () => ({stage: 'service'});
const question = (service, index) => {
  const q = SURVEY_QUESTIONS[service][index];
  return `<b>${q.question}</b>\n<i>${q.hint}</i>`;
};
function answerMessages(service, answers) {
  return SURVEY_QUESTIONS[service].map(q => `<b>${q.question}</b>\n${escapeHtml(answers[q.key] || '—')}`);
}
const validService = service => Object.hasOwn(PACKAGES, service || '');
const validBundle = (service, bundle) => validService(service) && Object.hasOwn(PACKAGES[service], bundle || '');

export function planUpdate(update, previous, env) {
  const callback = update.callback_query;
  const message = callback?.message || update.message;
  const user = callback?.from || message?.from;
  if (!message || !user || message.chat.type !== 'private') return null;
  const chatId = message.chat.id;
  let session = structuredClone(previous || initial());
  const effects = [];
  let order = null;
  // Keep the current bot screen in D1 so navigation can remove it after a restart.
  if (callback) effects.push({method: 'answerCallbackQuery', body: {callback_query_id: callback.id}});
  const oldMessages = new Set(previous?.ui_message_ids || []);
  if (callback?.message?.message_id) oldMessages.add(callback.message.message_id);
  for (const message_id of oldMessages) effects.push({method: 'deleteMessage', body: {chat_id: chatId, message_id}});
  const send = (text, reply_markup) => effects.push({method: 'sendMessage', trackUi: true, body: {chat_id: chatId, text, parse_mode: 'HTML', ...(reply_markup ? {reply_markup} : {})}});
  const reset = (prefix = '') => { session = initial(); send(prefix + welcome, mainMenu()); };
  const showPreview = () => {
    send(`<b>Проверьте вашу заявку:</b>\nУслуга: ${SERVICE_LABELS[session.service]}\nПакет: ${BUNDLE_LABELS[session.bundle]}\n\n${answerMessages(session.service, session.answers).join('\n\n')}\n\nВсё верно? Нажмите «Готово», чтобы отправить заявку.`, previewMenu());
  };
  const packages = service => {
    session = {stage: 'package', service};
    const buttons = BUNDLE_ORDER.map(bundle => button(BUNDLE_LABELS[bundle], `package:${service}:${bundle}`));
    send(`${SERVICE_LABELS[service]}\n\nВыберите пакет:`, kb([buttons.slice(0, 2), buttons.slice(2), [button('⬅️ Назад', 'back:main_menu')]]));
  };
  if (callback) {
    const data = callback.data || '';
    if (['back:main_menu', 'preview:cancel', 'survey:cancel'].includes(data)) reset(data.includes('cancel') ? '🗑 Черновик заявки удалён.\n\n' : '');
    else if (data.startsWith('service:') && validService(data.split(':')[1])) packages(data.split(':')[1]);
    else if (data.startsWith('back:packages:') && validService(data.split(':')[2])) packages(data.split(':')[2]);
    else if (data.startsWith('package:')) {
      const [, service, bundle] = data.split(':');
      if (!validBundle(service, bundle)) reset();
      else {
        session = {stage: 'view', service, bundle};
        const p = PACKAGES[service][bundle];
        effects.push({method: 'sendPhoto', trackUi: true, body: {chat_id: chatId, photo: `https://raw.githubusercontent.com/goshapa/goshapa-order-bot/main/images/${bundle}.jpg`, caption: `<b>${p.title}</b>\n<i>${p.subtitle}</i>`, parse_mode: 'HTML'}});
        send(`<b>Что входит в пакет:</b>\n${p.features.map(f => '• ' + f).join('\n')}\n\n<b>Что получает клиент:</b> ${p.gets}`, kb([[button('✅ Заказать', `order:start:${service}:${bundle}`)], [button('⬅️ Назад', `back:packages:${service}`)]]));
      }
    } else if (data.startsWith('order:start:')) {
      const [, , service, bundle] = data.split(':');
      if (!validBundle(service, bundle)) reset();
      else { session = {stage: 'survey', service, bundle, answers: {}, index: 0}; send(question(service, 0), cancelMenu()); }
    } else if (data === 'preview:restart' && session.stage === 'preview') {
      session = {...session, stage: 'survey', answers: {}, index: 0}; send(question(session.service, 0), cancelMenu());
    } else if (data === 'preview:confirm' && session.stage === 'preview') {
      const number = `GS-C${update.update_id}`;
      const now = new Date().toISOString();
      order = {order_number: number, telegram_id: user.id, username: user.username || null, full_name: [user.first_name, user.last_name].filter(Boolean).join(' '), service: session.service, bundle: session.bundle, answers: JSON.stringify(session.answers), created_at: now, updated_at: now, source_update: update.update_id};
      const contact = kb([[{text: '💬 Написать клиенту', url: user.username ? `https://t.me/${user.username}` : `tg://user?id=${user.id}`}]]);
      const header = `🆕 <b>Новая заявка ${number}</b>\n\n👤 Клиент: ${escapeHtml(order.full_name)}\nUsername: ${user.username ? '@' + escapeHtml(user.username) : '—'}\nTelegram ID: <code>${user.id}</code>\nУслуга: ${SERVICE_LABELS[session.service]}\nПакет: ${BUNDLE_LABELS[session.bundle]}\nДата (UTC): ${now}\nСтатус: New`;
      effects.push({method: 'sendMessage', body: {chat_id: env.ADMIN_ID, text: `${header}\n\n${answerMessages(session.service, session.answers).join('\n\n')}`, parse_mode: 'HTML', reply_markup: contact}});
      send(`✅ Заявка успешно отправлена! Программист Goshapa свяжется с вами в скором времени, чтобы обсудить дальнейшие детали проекта.\n\nНомер вашей заявки: <b>${number}</b>`, mainMenu());
      session = initial();
    } else send('Эта кнопка уже неактуальна. Начните с /start.', mainMenu());
  } else {
    const text = message.text?.trim();
    if (/^\/(start|cancel)(@\w+)?(?:\s|$)/.test(text || '')) reset(text.startsWith('/cancel') ? '❌ Текущее действие отменено.\n\n' : '');
    else if (session.stage === 'survey') {
      if (!text) send('Пожалуйста, ответьте текстовым сообщением.\n\n' + question(session.service, session.index), cancelMenu());
      else if (text.length > 500) send('Пожалуйста, сократите ответ до 500 символов.\n\n' + question(session.service, session.index), cancelMenu());
      else {
        session.answers[SURVEY_QUESTIONS[session.service][session.index].key] = text;
        session.index++;
        if (session.index < SURVEY_QUESTIONS[session.service].length) send(question(session.service, session.index), cancelMenu());
        else { session.stage = 'preview'; showPreview(); }
      }
    } else if (session.stage === 'preview') showPreview();
    else reset();
  }
  session.ui_message_ids = [];
  session.ui_update_id = update.update_id;
  return {chatId, session, effects, order};
}

async function deliver(row, env) {
  const effects = JSON.parse(row.effects);
  for (let i = row.cursor; i < effects.length; i++) {
    const effect = effects[i];
    const response = await fetch(`https://api.telegram.org/bot${env.BOT_TOKEN}/${effect.method}`, {method: 'POST', headers: {'content-type': 'application/json'}, body: JSON.stringify(effect.body), signal: AbortSignal.timeout(15000)});
    const result = await response.json();
    const harmlessDelete = effect.method === 'deleteMessage' && [400, 403].includes(result.error_code);
    if (!result.ok && effect.method !== 'answerCallbackQuery' && !harmlessDelete) {
      // Never log Telegram request URLs (they contain the token) or client answers.
      console.error('Telegram delivery failed', effect.method, result.error_code);
      throw new Error('Telegram delivery failed');
    }
    const saved = [env.DB.prepare('UPDATE deliveries SET cursor = ? WHERE update_id = ?').bind(i + 1, row.update_id)];
    if (effect.trackUi && result.ok && Number.isSafeInteger(result.result?.message_id)) {
      saved.push(env.DB.prepare("UPDATE sessions SET data = json_insert(data, '$.ui_message_ids[#]', ?) WHERE chat_id = ? AND json_extract(data, '$.ui_update_id') = ?").bind(result.result.message_id, effect.body.chat_id, row.update_id));
    }
    await env.DB.batch(saved);
  }
}

export default {
  async fetch(request, env) {
    const path = new URL(request.url).pathname;
    if (request.method === 'GET' && path === '/') return new Response('Goshapa order bot');
    if (request.method === 'GET' && path === '/health') {
      if (!env.DB || !env.BOT_TOKEN || !env.WEBHOOK_SECRET || !env.ADMIN_ID) return new Response('Not configured', {status: 503});
      try { await env.DB.prepare('SELECT count(*) AS n FROM sessions').first(); return Response.json({ok: true}); }
      catch { return new Response('Database unavailable', {status: 503}); }
    }
    if (request.method !== 'POST' || path !== '/telegram') return new Response('Not found', {status: 404});
    if (!env.WEBHOOK_SECRET || request.headers.get('X-Telegram-Bot-Api-Secret-Token') !== env.WEBHOOK_SECRET) return new Response('Forbidden', {status: 403});
    if (Number(request.headers.get('content-length')) > 262144) return new Response('Too large', {status: 413});
    let update;
    try { const body = await request.text(); if (body.length > 262144) return new Response('Too large', {status: 413}); update = JSON.parse(body); }
    catch { return new Response('Invalid JSON', {status: 400}); }
    if (!Number.isSafeInteger(update?.update_id)) return new Response('Invalid update', {status: 400});
    const owner = crypto.randomUUID();
    const now = Date.now();
    try {
      const lock = await env.DB.prepare("INSERT INTO locks(name,owner,expires) VALUES('telegram',?,?) ON CONFLICT(name) DO UPDATE SET owner=excluded.owner,expires=excluded.expires WHERE locks.expires < ? RETURNING owner").bind(owner, now + 240000, now).first();
      if (!lock) return new Response('Busy', {status: 503});
      let row = await env.DB.prepare('SELECT * FROM deliveries WHERE update_id = ?').bind(update.update_id).first();
      if (!row) {
        const message = update.callback_query?.message || update.message;
        const stored = message ? await env.DB.prepare('SELECT data FROM sessions WHERE chat_id = ?').bind(message.chat.id).first() : null;
        const plan = planUpdate(update, stored ? JSON.parse(stored.data) : null, env);
        if (!plan) return new Response('OK');
        const timestamp = new Date().toISOString();
        const statements = [];
        if (plan.order) {
          const o = plan.order;
          statements.push(env.DB.prepare('INSERT INTO orders(order_number,telegram_id,username,full_name,service,bundle,answers,created_at,updated_at,source_update) VALUES(?,?,?,?,?,?,?,?,?,?)').bind(o.order_number,o.telegram_id,o.username,o.full_name,o.service,o.bundle,o.answers,o.created_at,o.updated_at,o.source_update));
        }
        statements.push(env.DB.prepare('INSERT INTO sessions(chat_id,data,updated_at) VALUES(?,?,?) ON CONFLICT(chat_id) DO UPDATE SET data=excluded.data,updated_at=excluded.updated_at').bind(plan.chatId, JSON.stringify(plan.session), timestamp));
        statements.push(env.DB.prepare('INSERT INTO deliveries(update_id,effects,created_at) VALUES(?,?,?)').bind(update.update_id, JSON.stringify(plan.effects), timestamp));
        await env.DB.batch(statements);
        row = {update_id: update.update_id, effects: JSON.stringify(plan.effects), cursor: 0};
      }
      await deliver(row, env);
      return new Response('OK');
    } catch {
      console.error('Webhook processing failed', update.update_id);
      return new Response('Retry later', {status: 503});
    } finally {
      if (env.DB) await env.DB.prepare("DELETE FROM locks WHERE name = 'telegram' AND owner = ?").bind(owner).run();
    }
  },
};
